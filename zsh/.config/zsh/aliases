alias ll='ls -lah'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gs='git status'

alias d='dirs -v'
for index ({1..9}) alias "$index"="cd +${index}"; unset index

alias cdd="cd $DOTFILES"

# Better ssh if using kitty
[ "$TERM" = "xterm-kitty" ] && alias ssh="kitty +kitten ssh"
