#!/usr/bin/env bash

# Debian recommended fonts seems to make everything look better after its installation and reboot

# Install fonts
sudo apt install fonts-recommended -y

# Install liberation that mimics the look of some Microsoft fonts
sudo apt install fonts-liberation fonts-liberation2 -y
